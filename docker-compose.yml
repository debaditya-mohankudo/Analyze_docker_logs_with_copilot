services:

  log-analyzer-mcp:
    build: .
    container_name: log-analyzer-mcp
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    env_file:
      - .env
    environment:
      - DOCKER_HOST=unix:///var/run/docker.sock
    restart: unless-stopped
